describe("dataUploader", () => {
  test("uploads each resource in a separate request ", async () => {});

  test("uploads all linked data in a single request", async () => {});
  test("uploads all data and collects garbage when number of files reached the limit", async () => {});
  test("does not remove files when full upload of linked data fails", async () => {});
});
